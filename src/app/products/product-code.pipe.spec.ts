import { ProductCodePipe } from './product-code.pipe';

describe('ProductCodePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductCodePipe();
    expect(pipe).toBeTruthy();
  });
});
